upper_bound = input("Choose the range upper bound --> ")
upper_bound = int(input("Choose the range upper bound --> "))
for x in range(1, upper_bound):
    print("The next number in line is {}".format(exit(x)))